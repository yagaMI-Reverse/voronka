# Измерения

Прогон: `2026-07-28 20:47:47`  
Стенд: win32, Python 3.12.7, всё на 127.0.0.1  
amoCRM: **мок** (`mock_amo`), контроль дублей самой CRM выключен  
Ретраи: max 8 попыток, base 1.0s, cap 8.0s, полный джиттер

Воспроизвести: `python -m bench.run_all`

## A. Идемпотентность

| метрика | значение |
| --- | --- |
| отправлено заявок | 200 |
| из них уникальных людей | 140 |
| намеренных дублей | 60 |
| принято к обработке | 140 |
| отсечено как дубль | 60 |
| **сделок в CRM** | **140** |
| **лишних сделок** | **0** |
| контактов в CRM | 140 |
| очередь разобрана за | 0.94 с |

Дубли отправлялись НЕ байт-в-байт: телефон в другом формате (`+7…` → `8…`),
email в другом регистре, свой `request_id`. Дедуп работает по нормализованному
натуральному ключу, а не по идентификатору доставки.

## B. Отказ внешнего сервиса

amoCRM отдаёт 503 в течение 15 секунд, пока в неё едут 25 заявок.

### B1. Бюджет ретраев 8 попыток (значение по умолчанию)

| метрика | значение |
| --- | --- |
| бюджет ретраев | 8 попыток |
| заявок отправлено | 25 |
| CRM отвечает 503 | 15 с |
| ответов 503 отдано моком | 152 |
| приём заявок во время аварии | все 25 приняты за 0.03 с |
| сделок в CRM во время аварии | 0 |
| событий ушло в ретрай | 149 |
| доехало ретраями | 22 |
| исчерпало бюджет и легло в DLQ | 3 |
| довезено ручным replay из DLQ | 3 (за 0.37 с) |
| **сделок в CRM в итоге** | **25** |
| **потеряно событий** | **0** |
| осталось в DLQ | 0 |
| очередь разобрана за | 11.54 с |

**Что вскрылось:** бюджета в 8 попыток при cap 8.0s не хватило на аварию в 15 с — 3 события из 25 сожгли все попытки и легли в DLQ.
Для этого DLQ и существует: ручной replay довёз их, итог — 25 из 25, потерь 0.

### B2. Бюджет ретраев 14 попыток

| метрика | значение |
| --- | --- |
| бюджет ретраев | 14 попыток |
| заявок отправлено | 25 |
| CRM отвечает 503 | 15 с |
| ответов 503 отдано моком | 163 |
| приём заявок во время аварии | все 25 приняты за 0.06 с |
| сделок в CRM во время аварии | 0 |
| событий ушло в ретрай | 163 |
| доехало ретраями | 25 |
| исчерпало бюджет и легло в DLQ | 0 |
| довезено ручным replay из DLQ | 0 (за 0.0 с) |
| **сделок в CRM в итоге** | **25** |
| **потеряно событий** | **0** |
| осталось в DLQ | 0 |
| очередь разобрана за | 11.52 с |

**Вывод:** бюджет ретраев подбирается под ожидаемую длительность аварии, а не «на глаз». При 14 попытках ту же аварию пережили 25 из 25 без ручного вмешательства (в DLQ ушло 0).

## C. Время прохождения заявки

| метрика | p50 | p95 | max | среднее |
| --- | --- | --- | --- | --- |
| ответ формы (202 Accepted) | 0.7 мс | 1.0 мс | 6.7 мс | 0.9 мс |
| форма → карточка в CRM, **одиночная заявка** | 78.0 мс | 125.0 мс | 125.0 мс | 79.0 мс |
| форма → карточка в CRM, **залп 100 заявок разом** | 389 мс | 594 мс | 625 мс | 376.5 мс |

Выборка: 20 одиночных заявок и залп из 100 (100 доставок).

Две строки — не одно и то же, и путать их нельзя:

* **одиночная заявка** — то, что видит живой клиент: 202 сразу, карточка в CRM
  через один тик воркера + один запрос к API;
* **залп** — 100 заявок встают в очередь и разбираются одним воркером
  последовательно, поэтому p50 растёт: это плата за один воркер, а не за архитектуру.

Обе цифры сняты **против мока на localhost**: они показывают накладные расходы
самого конвейера. На боевом amoCRM сверху добавится сеть и время их API —
против живого аккаунта не измерялось.

## D. Одновременные дубли (гонка)

| метрика | значение |
| --- | --- |
| параллельных одинаковых отправок | 20 |
| принято | 1 |
| отсечено как дубль | 19 |
| **сделок в CRM** | **1** |

Дедуп через `INSERT OR IGNORE` по первичному ключу — победитель ровно один
даже когда все запросы приходят в одну миллисекунду.
